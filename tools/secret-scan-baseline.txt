# Accepted, reviewed findings. One "[rule] location:line" per line.
#
# Entries here are suppressed by tools/scan_secrets.py. Anything NOT listed
# still fails the scan, so this file must stay short and every line must have
# been looked at by a human.
#
# Blob SHAs are immutable, so a baselined history entry cannot silently start
# covering different content.
#
# ---------------------------------------------------------------------------
# 2026-08-18 - README.md as committed in 51e0a1d contained a documentation
# example of QiskitRuntimeService.save_account with the literal placeholder
# "<your token>". No real credential. The example was removed from the working
# tree so the pattern cannot be copy-pasted, but the original blob remains
# reachable in history and the two initial commits are not being rewritten.
[assigned-token] README.md (blob d6bb1388e):59
[qiskit-save-account] README.md (blob d6bb1388e):59
